<%= form_for @song  do |f| %>


<%= artist_select(@artist, @song) %>

  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <br/>

  <%= f.submit %>
<% end %>

<!-- if editing directly from song index:
  create a drop down of artist_select
elsif editing through artist
  display artist name:


artist_select(artist, song) -->
